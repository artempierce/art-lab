/**
 * RunsPage.tsx — placeholder for the Runs page (Phase 13, P13): a table of past runs, click a row to
 * see its trace (docs/contracts.md § 15). This ticket (P12) only needs Sidebar's "Runs" nav button
 * to have somewhere to go; Phase 13 replaces this file with the real page.
 */
export function RunsPage() {
  return (
    <main className="flex min-h-0 flex-col items-center justify-center bg-sage p-6">
      <p className="display text-2xl uppercase">Runs: coming in Phase 13</p>
    </main>
  )
}
